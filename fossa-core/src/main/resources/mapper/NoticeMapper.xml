<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.txy.mapper.NoticeMapper">

    <!-- top 为关键字，反引号 -->
    <sql id="Base_Column_List">
        id, title, cat, `top`, content, author, status, deleted, created_at
    </sql>

    <sql id="NotDeleted">deleted = 0</sql>

    <select id="selectAll" resultType="com.txy.entity.Notice">
        SELECT <include refid="Base_Column_List"/>
        FROM notice
        WHERE <include refid="NotDeleted"/>
        ORDER BY `top` DESC, created_at DESC
    </select>

    <select id="selectById" resultType="com.txy.entity.Notice">
        SELECT <include refid="Base_Column_List"/>
        FROM notice
        WHERE id = #{id} AND <include refid="NotDeleted"/>
        LIMIT 1
    </select>

    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM notice WHERE <include refid="NotDeleted"/>
    </select>

    <select id="countPublished" resultType="int">
        SELECT COUNT(*) FROM notice WHERE deleted = 0 AND status = '发布'
    </select>

    <insert id="insert" parameterType="com.txy.entity.Notice">
        INSERT INTO notice (id, title, cat, `top`, content, author, status, deleted)
        VALUES (#{id}, #{title}, #{cat}, #{top}, #{content}, #{author}, #{status}, 0)
    </insert>

    <update id="update" parameterType="com.txy.entity.Notice">
        UPDATE notice
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="cat != null">cat = #{cat},</if>
            <if test="top != null">`top` = #{top},</if>
            <if test="content != null">content = #{content},</if>
            <if test="author != null">author = #{author},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="logicDelete">
        UPDATE notice SET deleted = 1 WHERE id = #{id} AND deleted = 0
    </update>

</mapper>
