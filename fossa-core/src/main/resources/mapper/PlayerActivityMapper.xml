<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.txy.mapper.PlayerActivityMapper">

    <sql id="Base_Column_List">
        player_id, sessions, minutes, deleted, updated_at
    </sql>

    <sql id="NotDeleted">deleted = 0</sql>

    <select id="selectAll" resultType="com.txy.entity.PlayerActivity">
        SELECT <include refid="Base_Column_List"/>
        FROM player_activity
        WHERE <include refid="NotDeleted"/>
        ORDER BY minutes DESC
    </select>

    <select id="selectByPlayerId" resultType="com.txy.entity.PlayerActivity">
        SELECT <include refid="Base_Column_List"/>
        FROM player_activity
        WHERE player_id = #{playerId} AND <include refid="NotDeleted"/>
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.txy.entity.PlayerActivity">
        INSERT INTO player_activity (player_id, sessions, minutes, deleted, updated_at)
        VALUES (#{playerId}, #{sessions}, #{minutes}, 0, NOW())
    </insert>

    <update id="update" parameterType="com.txy.entity.PlayerActivity">
        UPDATE player_activity
        <set>
            <if test="sessions != null">sessions = #{sessions},</if>
            <if test="minutes != null">minutes = #{minutes},</if>
        </set>
        WHERE player_id = #{playerId} AND deleted = 0
    </update>

    <update id="logicDelete">
        UPDATE player_activity SET deleted = 1 WHERE player_id = #{playerId} AND deleted = 0
    </update>

</mapper>
