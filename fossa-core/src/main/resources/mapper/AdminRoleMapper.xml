<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.txy.mapper.AdminRoleMapper">

    <sql id="Role_Cols">
        role_key, role_name, remark, builtin, sort, deleted, created_at
    </sql>

    <resultMap id="roleMap" type="com.txy.entity.AdminRole">
        <id property="roleKey" column="role_key"/>
        <result property="roleName" column="role_name"/>
        <result property="remark" column="remark"/>
        <result property="builtin" column="builtin"/>
        <result property="sort" column="sort"/>
        <result property="deleted" column="deleted"/>
        <collection property="perms" ofType="java.lang.String" column="role_key"
                    select="selectPermsOfRole"/>
    </resultMap>

    <select id="selectRoles" resultMap="roleMap">
        SELECT <include refid="Role_Cols"/>
        FROM admin_role WHERE deleted = 0 ORDER BY sort ASC, role_key ASC
    </select>

    <select id="selectRole" resultMap="roleMap">
        SELECT <include refid="Role_Cols"/>
        FROM admin_role WHERE deleted = 0 AND role_key = #{roleKey} LIMIT 1
    </select>

    <insert id="insertRole">
        INSERT INTO admin_role (role_key, role_name, remark, builtin, sort, deleted)
        VALUES (#{roleKey}, #{roleName}, #{remark}, 0, #{sort}, 0)
    </insert>

    <update id="updateRole">
        UPDATE admin_role SET role_name = #{roleName}, remark = #{remark}, sort = #{sort}
        WHERE role_key = #{roleKey} AND deleted = 0
    </update>

    <update id="softDeleteRole">
        UPDATE admin_role SET deleted = 1 WHERE role_key = #{roleKey} AND builtin = 0
    </update>

    <delete id="deletePerms">
        DELETE FROM admin_role_perm WHERE role_key = #{roleKey}
    </delete>

    <insert id="insertPerms">
        INSERT INTO admin_role_perm (role_key, perm_code) VALUES
        <foreach collection="perms" item="p" separator=",">(#{roleKey}, #{p})</foreach>
    </insert>

    <select id="selectPerms" resultType="com.txy.entity.AdminPerm">
        SELECT perm_code, perm_name, module, sort FROM admin_perm ORDER BY module ASC, sort ASC
    </select>

    <select id="selectPermsOfRole" resultType="java.lang.String">
        SELECT perm_code FROM admin_role_perm WHERE role_key = #{roleKey}
    </select>

    <select id="countAccountByRole" resultType="int">
        SELECT COUNT(*) FROM admin_account WHERE deleted = 0 AND role = #{roleKey}
    </select>

</mapper>
